diff --git a/node_modules/expo/node_modules/@expo/cli/build/src/utils/open.js b/node_modules/expo/node_modules/@expo/cli/build/src/utils/open.js
index bcbab1a..3646be8 100644
--- a/node_modules/expo/node_modules/@expo/cli/build/src/utils/open.js
+++ b/node_modules/expo/node_modules/@expo/cli/build/src/utils/open.js
@@ -250,9 +250,12 @@ async function spawnWindowsStartAsync(target, browserApp, browserArgs) {
         '""'
     ];
     if (browserApp) startArgs.push(browserApp);
-    startArgs.push(target, ...browserArgs);
+    // The URL contains `&` which cmd.exe's `start` treats as a command
+    // separator unless the whole URL is wrapped in quotes.
+    startArgs.push(`"${target}"`, ...browserArgs);
     await (0, _spawnasync().default)(cmd, startArgs, {
-        stdio: 'ignore'
+        stdio: 'ignore',
+        windowsVerbatimArguments: true
     });
 }
 function isWsl() {
